# FUN_1000_3830 - 메인 게임 루프

**주소**: 1000:3830
**크기**: 447 bytes
**복잡도**: 매우 높음
**카테고리**: 게임 로직
**분석일**: 2025-11-24

---

## 📊 개요

**기능**: 메인 게임 로직 및 상태 관리. 재귀 호출을 통해 실제 게임 루프를 구현.

**특징**:
- 재귀 호출 구조 (LAB_1000_024b에서 자기 자신 호출)
- 게임 상태 머신
- VGA 동기화
- 레벨 전환 처리
- 플레이어 생존 체크

**호출자**:
- entry() (초기 진입)
- 자기 자신 (재귀)

**호출하는 함수**: 16개
- FUN_1000_3a20() - 초기화
- FUN_1000_0360() - 객체 업데이트
- FUN_1000_03ca() - 객체 처리
- (+ 13개 더)

---

## 🔍 상세 분석

### 함수 구조

```c
void FUN_1000_3830(void) {
  // 1. 초기화 및 상태 체크
  FUN_1000_3a20();

  // 2. 게임 상태별 분기
  if (*(char *)0x38d1 == 0x01) {
    // 일반 상태
  } else {
    // 특수 상태 (레벨 전환 등)
  }

  // 3. 메인 게임 루프 (LAB_1000_024b)
  do {
    // 프레임 타이밍
    while (*(byte *)0x318a < 0xf);
    *(undefined2 *)0x318a = 0;

    // 매 프레임 실행
    FUN_1000_3830();  // 재귀!
    FUN_1000_0360();  // 객체 업데이트
    // ... 14개 함수 더

  } while (*(char *)0x318e != '\0');
}
```

### 상태 다이어그램

```
[일반 게임]
    ↓
*(int *)0x38d3 == 999?
    ↓ Yes
[레벨 전환]
    ↓
카운터 0→6
    ↓
[플레이어 생존 체크]
    ↓ 살아있음
[플레이어 초기화]
    ↓
[VGA 동기화]
    ↓
[메인 루프]
```

### 중요 메모리 주소

| 주소 | 타입 | 용도 | 읽기/쓰기 |
|------|------|------|-----------|
| 0x16c8 | byte | 플레이어1 상태 | R/W |
| 0x16e0 | byte | 플레이어2 상태 | R/W |
| 0x38d0 | byte | 레벨 전환 카운터 (0~6) | R/W |
| 0x38d1 | byte | 게임 상태 플래그 | R/W |
| 0x38d3 | word | 특수 상태 (999=레벨전환) | R |
| 0x318a | word | 프레임 타이밍 (0~15) | R/W |
| 0x318e | byte | 게임 종료 플래그 | R |
| 0x16b0 | word | 프레임 카운터 | W |

### 메인 루프 (LAB_1000_024b)

**매 프레임 실행되는 16개 함수**:

```c
LAB_1000_024b:
do {
  // 프레임 타이밍 (15 대기)
  do {} while (*(byte *)0x318a < 0xf);
  *(undefined2 *)0x318a = 0;

  FUN_1000_3830();  // #1  - 게임 상태 관리
  FUN_1000_0360();  // #2  - 객체 업데이트 ⭐
  FUN_1000_03ca();  // #3  - 객체 처리
  FUN_1000_20fb();  // #4  - 객체 처리 (다른 타입)
  FUN_1000_039e();  // #5  - 거리 체크
  FUN_1000_211b();  // #6
  FUN_1000_029a();  // #7
  FUN_1000_03ea();  // #8
  FUN_1000_213b();  // #9
  FUN_1000_8135();  // #10 - 함수 포인터 디스패처
  FUN_1000_034b();  // #11
  FUN_1000_462a();  // #12
  FUN_1000_0518();  // #13
  FUN_1000_4640();  // #14
  FUN_1000_3fe0();  // #15
  *(int *)0x16b0 = *(int *)0x16b0 + 1;  // 프레임 카운터
  FUN_1000_05e9();  // #16

} while (*(char *)0x318e != '\0');
```

### VGA 동기화

```c
// VGA 수직 블랭킹 대기
do {
  bVar1 = in(0x3da);  // VGA 상태 레지스터
} while ((bVar1 & 8) == 0);  // 비트 8: 수직 블랭킹

do {
  bVar1 = in(0x3da);
} while ((bVar1 & 8) != 0);  // 블랭킹 종료 대기
```

**목적**: 화면 깜빡임 방지

---

## 💡 핵심 발견

### 1. 재귀 호출 구조
- 진입점에서 한 번 호출
- LAB_1000_024b에서 자기 자신을 다시 호출
- 실제 게임 루프는 LAB_1000_024b

### 2. 프레임 레이트 제어
- `0x318a` 카운터가 15 도달까지 대기
- VGA 동기화로 화면 갱신
- 예상 FPS: 약 4-6 (확인 필요)

### 3. 레벨 전환 로직
- `0x38d3 == 999` 일 때 레벨 전환 시작
- 카운터 `0x38d0`이 0→6까지 증가
- 6 도달 시 플레이어 초기화

### 4. 게임 오버 처리
- `0x16c8` (P1 상태) == 0 → 게임 오버
- `0x16e0` (P2 상태) 체크
- `FUN_1000_4780()` 게임 오버 화면

---

## 🔗 관련 함수

### 직접 호출하는 함수 (주요)
- [FUN_1000_0360](FUN_1000_0360.md) - 객체 업데이트
- FUN_1000_3a20 - 초기화
- FUN_1000_39ee - 일반 게임 로직
- FUN_1000_4780 - 게임 오버 화면
- FUN_1000_599d - 화면 업데이트

### 상위 문서
- [메인 루프 완전 분석](../MAIN_LOOP_COMPLETE_ANALYSIS.md)
- [호출 그래프](../CALL_GRAPH.md)

---

## 📌 TODO

- [ ] 프레임 레이트 정확히 측정
- [ ] 나머지 14개 함수 각각 분석
- [ ] FUN_1000_3a20 초기화 로직 파악
- [ ] 게임 오버 화면 분석
- [ ] 레벨 전환 완전 분석

---

## 🧪 검증 방법

```bash
# DOSBox에서 실행하며 메모리 관찰
# 0x38d3 값이 999가 되는 시점
# 0x38d0 카운터 증가 확인
```

---

**디컴파일 파일**: `output/decompiled/FUN_1000_3830.c`
**최종 분석일**: 2025-11-24
**분석자**: Claude Code
**신뢰도**: 85% (프레임 타이밍 제외하고 이해 완료)
