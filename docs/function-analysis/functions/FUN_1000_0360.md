# FUN_1000_0360 - 객체 업데이트 루프

**주소**: 1000:0360
**크기**: 62 bytes
**복잡도**: 중간
**카테고리**: 게임 로직
**분석일**: 2025-11-24

---

## 📊 개요

**기능**: 7개 게임 객체(플레이어, 적)를 순회하며 업데이트 및 렌더링

**특징**:
- 간단한 for 루프
- 객체 배열 순회 (0x16c6 ~ 0x176e)
- 24 bytes 간격
- 작업 버퍼 사용 패턴

**호출자**:
- FUN_1000_3830() - 메인 루프 (매 프레임)

**호출하는 함수**: 5개
- FUN_1000_0412() - 객체 데이터 복사
- FUN_1000_16e0() - 타입별 업데이트
- FUN_1000_039e() - 거리 체크
- FUN_1000_0e30() - 상태별 행동
- FUN_1000_3e6d() - 렌더링 및 상태 관리
- FUN_1000_041b() - 데이터 저장

---

## 🔍 상세 분석

### 함수 구조

```c
void FUN_1000_0360(void) {
  undefined2 addr = 0x16c6;  // 시작 주소

  do {
    // 활성화된 객체만 처리
    if ((addr < 0x16f6) || (*(char *)(addr + 2) != '\0')) {

      // 1. 객체 → 작업 버퍼
      FUN_1000_0412();  // 24 bytes 복사: addr → 0x168f

      // 2. 업데이트 로직
      if (*(char *)0x1691 != '\0') {  // 활성화됨?

        // 타입별 업데이트
        FUN_1000_16e0();  // 함수 포인터 디스패처

        // 플레이어용 거리 체크
        if (addr < 0x16f6) {
          FUN_1000_039e();
        }

        // 상태별 행동
        FUN_1000_0e30();  // 함수 포인터 디스패처
      }

      // 3. 렌더링 및 상태 관리
      FUN_1000_3e6d();

      // 4. 작업 버퍼 → 객체
      FUN_1000_041b();  // 24 bytes 복사: 0x168f → addr
    }

    addr = addr + 0x18;  // 다음 객체 (24 bytes)

  } while (addr != 0x176e);  // 끝 주소
}
```

### 7개 객체 배열

**메모리 레이아웃**:
```
0x16c6  ┌──────────────┐
        │ 객체 #0      │  24 bytes  (플레이어1?)
0x16de  ├──────────────┤
        │ 객체 #1      │  24 bytes  (플레이어2?)
0x16f6  ├──────────────┤ ← 경계 (0x16f6)
        │ 객체 #2      │  24 bytes  (적?)
0x170e  ├──────────────┤
        │ 객체 #3      │  24 bytes  (적?)
0x1726  ├──────────────┤
        │ 객체 #4      │  24 bytes  (적?)
0x173e  ├──────────────┤
        │ 객체 #5      │  24 bytes  (적?)
0x1756  ├──────────────┤
        │ 객체 #6      │  24 bytes  (적?)
0x176e  └──────────────┘
```

**추정 역할**:
- 객체 #0, #1: 플레이어 1, 2 (0x16f6 미만)
- 객체 #2~6: 적 캐릭터 5개

### 객체 처리 파이프라인

```
for (addr = 0x16c6; addr < 0x176e; addr += 0x18) {

  ┌─────────────────────────────────────┐
  │ 1. 복사 (객체 → 작업 버퍼)          │
  │    FUN_1000_0412()                  │
  │    24 bytes: addr → 0x168f          │
  └─────────────────────────────────────┘
                  ↓
  ┌─────────────────────────────────────┐
  │ 2. 업데이트 로직                     │
  │    FUN_1000_16e0() - 타입별         │
  │    FUN_1000_039e() - 거리 (P1/P2)   │
  │    FUN_1000_0e30() - 상태별         │
  └─────────────────────────────────────┘
                  ↓
  ┌─────────────────────────────────────┐
  │ 3. 렌더링 및 상태                    │
  │    FUN_1000_3e6d()                  │
  │    └─ FUN_1000_3f71()               │
  │       └─ FUN_1000_12c0() - Blit!    │
  └─────────────────────────────────────┘
                  ↓
  ┌─────────────────────────────────────┐
  │ 4. 저장 (작업 버퍼 → 객체)          │
  │    FUN_1000_041b()                  │
  │    24 bytes: 0x168f → addr          │
  └─────────────────────────────────────┘
}
```

### 작업 버퍼 (0x168f)

**목적**: 현재 처리중인 객체 데이터 임시 저장

**구조** (추정 24+ bytes):
| 오프셋 | 주소 | 크기 | 추정 필드 | 참조 함수 |
|--------|------|------|-----------|-----------|
| +0x00 | 0x168f | 1 byte | 객체 타입 | FUN_1000_16e0 |
| +0x02 | 0x1691 | 1 byte | 객체 상태 | FUN_1000_0e30 |
| +0x03 | 0x1692 | 1 byte | ? | FUN_1000_3f71 |
| +0x05 | 0x1694 | 1 byte | 타이머? | FUN_1000_3e6d |
| +0x06 | 0x1695 | 2 bytes | 스프라이트 ID? | FUN_1000_3e6d |
| +0x08 | 0x1697 | 2 bytes | X 좌표 | FUN_1000_3f71 |
| +0x0a | 0x1699 | 2 bytes | Y 좌표 | FUN_1000_3f71 |
| ... | ... | ... | ... | ... |
| +0x18 | 0x16a7 | 2 bytes | 계산된 X | FUN_1000_3f71 |
| +0x1a | 0x16a9 | 2 bytes | 계산된 Y | FUN_1000_3f71 |
| +0x1f | 0x16ae | 1 byte | 충돌 플래그? | FUN_1000_12c0 |

### 함수 포인터 디스패처

**FUN_1000_16e0** - 객체 타입별 업데이트:
```c
(*(code *)*(undefined2 *)(*(byte *)0x168f + 0x16ef))();
```
- 인덱스: `0x168f` (객체 타입)
- 테이블: `0x16ef`
- 용도: 플레이어/적/무기별 다른 로직

**FUN_1000_0e30** - 객체 상태별 행동:
```c
(*(code *)*(undefined2 *)(*(byte *)0x1691 + 0xe3f))();
```
- 인덱스: `0x1691` (객체 상태)
- 테이블: `0xe3f`
- 용도: Idle/Walk/Attack/Jump 등

---

## 💡 핵심 발견

### 1. 7개 객체 시스템
- 고정된 배열 크기
- 플레이어 2개 + 적 5개
- 24 bytes 구조체

### 2. 작업 버퍼 패턴
- 복사 → 처리 → 저장
- 메모리 일관성 유지
- 안전한 업데이트

### 3. 플레이어 우선 처리
- `addr < 0x16f6` 조건
- 플레이어만 `FUN_1000_039e()` 호출
- 거리 체크/충돌 감지?

### 4. 함수 포인터 기반 OOP
- 타입별 가상 함수
- 상태별 가상 함수
- C로 구현된 vtable 패턴

---

## 🔗 관련 함수

### 호출하는 함수
- [FUN_1000_0412] - 객체 데이터 복사 (9 bytes)
- [FUN_1000_16e0] - 타입별 업데이트 디스패처
- [FUN_1000_039e] - 거리 체크
- [FUN_1000_0e30] - 상태별 행동 디스패처
- [FUN_1000_3e6d] - 렌더링 및 상태 관리
- [FUN_1000_041b] - 데이터 저장

### 호출되는 곳
- [FUN_1000_3830](FUN_1000_3830.md) - 메인 루프

### 상위 문서
- [메인 루프 완전 분석](../MAIN_LOOP_COMPLETE_ANALYSIS.md)
- [함수 포인터](../FUNCTION_POINTERS.md)

---

## 📌 TODO

- [ ] 객체 구조체 24 bytes 완전 분석
- [ ] FUN_1000_16e0 테이블 내용 확인
- [ ] FUN_1000_0e30 테이블 내용 확인
- [ ] FUN_1000_039e 정확한 기능 파악
- [ ] 플레이어 vs 적 구분 기준 명확히

---

## 🧪 검증 방법

```python
# 객체 배열 검증
base = 0x16c6
count = 7
size = 0x18

for i in range(count):
    addr = base + (i * size)
    print(f"객체 #{i}: 0x{addr:04x}")
    # 메모리 덤프로 확인

# 예상 출력:
# 객체 #0: 0x16c6
# 객체 #1: 0x16de
# 객체 #2: 0x16f6
# ...
```

---

**디컴파일 파일**: `output/decompiled/FUN_1000_0360.c`
**최종 분석일**: 2025-11-24
**분석자**: Claude Code
**신뢰도**: 90% (구조 완전 이해)
