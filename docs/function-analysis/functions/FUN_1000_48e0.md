# FUN_1000_48e0 - 스프라이트 렌더링 디스패처

**주소**: 1000:48e0
**크기**: 4 bytes
**카테고리**: Rendering (함수 포인터 디스패처)
**중요도**: ⭐⭐⭐

---

## 📋 목적

**스프라이트 타입별 렌더링 함수를 동적으로 호출**하는 디스패처.
- 메모리 주소 **0x18c6**에 저장된 함수 포인터를 호출
- 간접 점프 테이블 패턴 (indirect jump table)
- OOP의 가상 함수 테이블(vtable)과 유사한 역할

---

## 🔗 호출 관계

**호출자**:
- FUN_1000_029a (메인 루프) ⭐
- FUN_1000_3094
- FUN_1000_30ac
- FUN_1000_3830 (메인 게임 로직)
- FUN_1000_4660
- FUN_1000_4780
- FUN_1000_489a

**호출 함수**:
- `*(undefined2 *)0x18c6` 주소에 저장된 함수 (동적)

---

## 📊 코드 분석

```c
void FUN_1000_48e0(void) {
  /* WARNING: Could not recover jumptable at 0x000148e0. Too many branches */
  /* WARNING: Treating indirect jump as call */
  (*(code *)*(undefined2 *)0x18c6)();
  return;
}
```

### 코드 해석

```c
(*(code *)*(undefined2 *)0x18c6)();
```

이것은 다음과 같이 동작합니다:
1. `*(undefined2 *)0x18c6` - 메모리 주소 0x18c6에서 2바이트(워드) 읽기
2. `(code *)` - 읽은 값을 함수 포인터로 캐스팅
3. `(...)()` - 해당 함수 호출

**예시**:
```
메모리 0x18c6에 저장된 값: 0x1234
→ 함수 주소 0x1234를 호출
```

---

## 🎯 호출 맥락 (FUN_1000_029a에서)

```c
// 스프라이트 주소 설정
DAT_1988_4664 = *(undefined2 *)(iVar2 + 2);  // 스프라이트 주소 저장

// 추가 파라미터 설정
*puVar4 = *(undefined2 *)(iVar3 + 0x10);
puVar4 = puVar4 + 1;

// 렌더링 함수 디스패치
FUN_1000_48e0();  // ← 0x18c6의 함수 포인터 호출
```

**순서**:
1. 스프라이트 주소를 `DAT_1988_4664`에 저장
2. 기타 렌더링 파라미터 설정
3. **FUN_1000_48e0** 호출 → 실제 렌더링 함수 실행

---

## 💡 함수 포인터 테이블 패턴

이 패턴은 1980년대 C 코드에서 OOP를 구현하는 전형적인 방법입니다:

### 다형성 구현 (Polymorphism in C)

```c
// 현대 C++의 가상 함수
class Sprite {
  virtual void render() = 0;
};

// 1988 DOS의 C 구현
typedef void (*RenderFunc)(void);
RenderFunc sprite_render = *(RenderFunc *)0x18c6;
sprite_render();  // 동적 디스패치
```

### 게임에서의 활용

**추정되는 시나리오**:
```c
// 초기화 시점에 함수 포인터 설정
if (sprite_type == PLAYER) {
  *(uint16_t *)0x18c6 = (uint16_t)&render_player;
}
else if (sprite_type == ENEMY) {
  *(uint16_t *)0x18c6 = (uint16_t)&render_enemy;
}
else if (sprite_type == WEAPON) {
  *(uint16_t *)0x18c6 = (uint16_t)&render_weapon;
}

// 메인 루프에서 호출
FUN_1000_48e0();  // 현재 스프라이트 타입에 맞는 함수 실행
```

---

## 🔍 발견 사항

### Ghidra 디컴파일 한계

```
WARNING: Could not recover jumptable at 0x000148e0. Too many branches
WARNING: Treating indirect jump as call
```

- **이유**: 간접 점프는 여러 곳으로 분기 가능
- **Ghidra의 선택**: 함수 호출로 처리 (정확한 판단!)
- **결과**: 4바이트짜리 매우 간단한 함수로 디컴파일

### 실제 어셈블리 (추정)

```asm
FUN_1000_48e0:
    mov  bx, word ptr [0x18c6]  ; 함수 포인터 로드
    jmp  bx                      ; 간접 점프
```

또는:

```asm
    call word ptr [0x18c6]       ; 간접 호출
    ret
```

---

## ❓ 미해결 문제

1. **0x18c6 함수 포인터 설정 위치**
   - 어디서 초기화되는가?
   - 언제 변경되는가?
   - 가능한 함수 주소 목록은?

2. **실제 렌더링 함수들**
   - FUN_1000_12c0이 이 테이블에 포함되는가?
   - 다른 렌더링 함수는?

3. **스프라이트 타입 구분**
   - LINDA, ABOBO, PLAYER1, WEAPONS 각각 다른 함수?
   - 아니면 공통 함수 + 파라미터?

---

## 🎯 다음 단계

1. **메모리 0x18c6 초기화 코드 찾기**
   - entry.c 또는 초기화 함수 확인
   - 어셈블리 직접 확인 필요

2. **함수 포인터 후보 목록 작성**
   - 렌더링 관련 함수 모두 수집
   - 주소 범위 추정

3. **FUN_1000_599d와 비교**
   - 0x18c6 vs 0x18d8
   - 두 테이블의 관계는?

4. **DOSBox 디버거 사용**
   - 런타임에 0x18c6 값 확인
   - 실제 호출되는 함수 주소 추적

---

**분석일**: 2025-11-24
**분석 상태**: ✅ 100% 완료 (패턴 이해)
**구현 필요**: 함수 포인터 테이블 역공학
