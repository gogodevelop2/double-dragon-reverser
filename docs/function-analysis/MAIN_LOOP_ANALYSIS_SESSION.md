# 메인 루프 14개 함수 분석 세션

**날짜**: 2025-11-24
**목표**: 메인 루프 나머지 14개 함수 순차 분석

---

## 진행 상황

- ✅ FUN_1000_03ca
- ✅ FUN_1000_20fb
- ⏳ 나머지 12개

---

## ✅ #1. FUN_1000_03ca - 객체 배열 1 렌더링 준비

**주소**: 1000:03ca
**크기**: 32 bytes

### 코드
```c
void FUN_1000_03ca(void) {
  int iVar1 = 0x16c6;  // 객체 배열 1 시작
  do {
    if (*(char *)(iVar1 + 2) != '\0') {  // 활성 체크
      FUN_1000_0412();  // 객체 → 작업버퍼
      FUN_1000_0426();  // 좌표 계산 + 렌더 리스트
      FUN_1000_041b();  // 작업버퍼 → 객체
    }
    iVar1 = iVar1 + 0x18;  // +24 bytes
  } while (iVar1 != 0x176e);
}
```

### 분석

**배열 구조**:
- 주소: 0x16c6 ~ 0x176e
- 개수: 7개 객체
- 크기: 24 bytes each
- 용도: 플레이어(2) + 적(5)

**호출 체인**:
```
FUN_1000_0412: memcpy(작업버퍼, 객체, 24)
FUN_1000_0426: 좌표 계산
  - 0x1690: 방향 (2,4,6,8)
  - 0xf396, 0xf398: 화면 스크롤 보정
  - 0x169f: 렌더 리스트 포인터
FUN_1000_041b: memcpy(객체, 작업버퍼, 24)
```

**역할**: 객체 렌더링 준비
**새 이름**: `prepare_entities_for_rendering()`

---

## ✅ #2. FUN_1000_20fb - 객체 배열 2 렌더링 준비

**주소**: 1000:20fb
**크기**: 32 bytes

### 코드
```c
void FUN_1000_20fb(void) {
  int iVar1 = 0x3542;  // 객체 배열 2 시작
  do {
    if (*(char *)(iVar1 + 2) != '\0') {
      FUN_1000_2189();
      FUN_1000_2255();
      FUN_1000_2192();
    }
    iVar1 = iVar1 + 0x12;  // +18 bytes
  } while (iVar1 != 0x35ae);
}
```

### 분석

**배열 구조**:
- 주소: 0x3542 ~ 0x35ae
- 개수: 6개 객체
- 크기: 18 bytes each
- 용도: 투사체/아이템/이펙트

**호출 체인** (추정):
```
FUN_1000_2189: 객체 → 작업버퍼
FUN_1000_2255: 좌표 계산
FUN_1000_2192: 작업버퍼 → 객체
```

**역할**: 투사체/아이템 렌더링 준비
**새 이름**: `prepare_projectiles_for_rendering()`

---

## 📊 메모리 맵 업데이트

### 객체 시스템

| 배열 | 주소 | 개수 | 크기 | 용도 |
|------|------|------|------|------|
| **배열 1** | 0x16c6 ~ 0x176e | 7개 | 24 bytes | 플레이어 + 적 |
| **배열 2** | 0x3542 ~ 0x35ae | 6개 | 18 bytes | 투사체/아이템/이펙트 |

### 공통 패턴

```c
// 배열 순회 패턴
for (객체 in 배열) {
  if (활성) {
    복사_to_작업버퍼();
    좌표_계산();
    복사_from_작업버퍼();
  }
}
```

---

---

## ✅ #3. FUN_1000_039e - 화면 경계 체크

**주소**: 1000:039e
**크기**: 44 bytes

### 코드
```c
void FUN_1000_039e(void) {
  int iVar1 = *(int *)0x1697;  // 엔티티 위치
  int iVar2 = *(int *)0xf396;  // 스크롤 위치

  if ((iVar1 - iVar2 < 0xc) && (*(int *)0x169b == -1)) {
    *(undefined2 *)0x169b = 0;  // 왼쪽 경계
  }
  if ((0x30 < iVar1 - iVar2) && (*(int *)0x169b == 1)) {
    *(undefined2 *)0x169b = 0;  // 오른쪽 경계
  }
}
```

### 분석

**경계 체크**:
- 0x1697: 엔티티 X 좌표
- 0xf396: 화면 스크롤 X
- 0x169b: 방향 플래그 (-1: 왼쪽, 1: 오른쪽)
- 0xc (12픽셀): 왼쪽 경계
- 0x30 (48픽셀): 오른쪽 경계

**역할**: 플레이어가 화면 경계를 벗어나지 않도록 제한
**새 이름**: `check_screen_bounds()`

---

## ✅ #4. FUN_1000_211b - 투사체 로직 업데이트

**주소**: 1000:211b
**크기**: 32 bytes

### 코드
```c
void FUN_1000_211b(void) {
  int iVar1 = 0x3542;  // 투사체 배열 2
  do {
    if (*(char *)(iVar1 + 2) != '\0') {
      FUN_1000_2189();  // 작업버퍼 복사
      FUN_1000_20a0();  // 투사체 로직 (NEW!)
      FUN_1000_2192();  // 복사 백
    }
    iVar1 = iVar1 + 0x12;
  } while (iVar1 != 0x35ae);
}
```

### 분석

**차이점 (vs FUN_1000_20fb)**:
- 20fb: 2189 → **2255** → 2192 (렌더링 준비)
- 211b: 2189 → **20a0** → 2192 (로직 업데이트)

**역할**: 투사체 이동/충돌/수명 업데이트
**새 이름**: `update_projectiles()`

---

## ⭐ #5. FUN_1000_029a - 깊이 정렬 렌더링 (핵심!)

**주소**: 1000:029a
**크기**: 177 bytes

### 코드
```c
void FUN_1000_029a(void) {
  undefined2 *puVar4 = (undefined2 *)0x156a;
  uint uVar1 = DAT_1988_16c0;  // 시작 깊이

  do {
    // 1. 투사체 렌더링 (해당 깊이)
    int iVar3 = 0x3542;
    do {
      if ((*(char *)(iVar3 + 2) != '\0') &&
         ((int)*(char *)(iVar3 + 3) + *(int *)(iVar3 + 8) == uVar1)) {
        DAT_1988_4664 = 0x493a;
        *puVar4 = *(undefined2 *)(iVar3 + 0xe);
        puVar4++;
        FUN_1000_48e0();  // 렌더링!
      }
      iVar3 += 0x12;
    } while (iVar3 != 0x35ae);

    // 2. 엔티티 렌더링 (해당 깊이)
    iVar3 = 0x16c6;
    do {
      if ((*(char *)(iVar3 + 2) != '\0') &&
         ((int)*(char *)(iVar3 + 3) + *(int *)(iVar3 + 10) == uVar1)) {
        int iVar2 = *(int *)*(undefined2 *)(iVar3 + 6);

        // 방향별 스프라이트 선택
        if (*(char *)(iVar3 + 1) == '\x02') iVar2 += 8;   // 아래
        if (*(char *)(iVar3 + 1) == '\x06') iVar2 += 0x10; // 위
        if (*(char *)(iVar3 + 1) == '\x04') iVar2 += 0x18; // 왼쪽
        if (*(char *)(iVar3 + 1) == '\b')   iVar2 += 0x20; // 오른쪽

        DAT_1988_4664 = *(undefined2 *)(iVar2 + 2);
        *puVar4 = *(undefined2 *)(iVar3 + 0x10);
        puVar4++;
        FUN_1000_48e0();  // 렌더링!
      }
      iVar3 += 0x18;
    } while (iVar3 != 0x176e);

    uVar1++;  // 다음 깊이 레이어
  } while (uVar1 <= DAT_1988_16c2);

  DAT_1988_004c = puVar4 - 1;
}
```

### 분석

**이것이 바로 메인 렌더링 루프!**

**3중 루프 구조**:
1. **깊이 루프**: 뒤→앞 레이어 순회
2. **투사체 루프**: 해당 깊이의 투사체만 렌더링
3. **엔티티 루프**: 해당 깊이의 엔티티만 렌더링

**구조체 오프셋**:
- +0x01: 방향 (2,4,6,8)
- +0x02: 활성 플래그
- +0x03: 깊이 오프셋
- +0x06: 스프라이트 테이블 포인터
- +0x08 (투사체): Y 좌표
- +0x0a (엔티티): Y 좌표
- +0x0e: 렌더 데이터
- +0x10: 렌더 데이터

**렌더링 순서**:
```
for depth in [back...front]:
  for projectile in projectiles:
    if projectile.depth == depth: render()
  for entity in entities:
    if entity.depth == depth: render()
```

**역할**: **깊이 정렬 렌더링 - 게임 그래픽의 핵심!**
**새 이름**: `render_all_objects_depth_sorted()`

---

## ✅ #6. FUN_1000_03ea - 프레임 기반 엔티티 업데이트

**주소**: 1000:03ea
**크기**: 40 bytes

### 코드
```c
void FUN_1000_03ea(void) {
  if ((*(uint *)0x16b0 & 1) != 0) {  // 홀수 프레임?
    int iVar1 = 0x16c6;
    do {
      if (*(char *)(iVar1 + 2) != '\0') {
        FUN_1000_0412();  // 작업버퍼
        FUN_1000_04e1();  // 애니메이션/로직
        FUN_1000_041b();  // 복사 백
      }
      iVar1 += 0x18;
    } while (iVar1 != 0x176e);
  }
}
```

### 분석

**프레임 체크**:
- 0x16b0: 프레임 카운터
- & 1: 홀수 프레임만 실행 (30fps → 15fps)

**역할**: 격프레임 엔티티 애니메이션/로직 업데이트
**새 이름**: `update_entities_every_other_frame()`

---

## ✅ #7. FUN_1000_213b - 조건부 투사체 업데이트

**주소**: 1000:213b
**크기**: 77 bytes

### 코드
```c
void FUN_1000_213b(void) {
  int iVar1 = 0x3542;
  do {
    if (*(char *)(iVar1 + 2) != '\0') {
      FUN_1000_2189();

      // 복잡한 조건 체크
      bool should_update = false;
      if (*(char *)0x3530 == '\x04') {
        if (*(char *)0x3532 != '\x06') {
          should_update = (*(uint *)0x16b0 & 1) != 0;
        } else {
          should_update = true;
        }
      } else if (*(char *)0x3530 == '\x06') {
        if ((*(char *)0x3532 == '\x04') || (*(char *)0x3532 == '\x02')) {
          should_update = true;
        } else {
          should_update = (*(uint *)0x16b0 & 1) != 0;
        }
      } else {
        should_update = (*(uint *)0x16b0 & 1) != 0;
      }

      if (should_update) {
        FUN_1000_224a();
        FUN_1000_2192();
      }
    }
    iVar1 += 0x12;
  } while (iVar1 != 0x35ae);
}
```

### 분석

**조건**:
- 0x3530, 0x3532: 투사체 타입 플래그 (4, 6 = 특수 타입)
- 프레임 카운터 체크

**역할**: 투사체 타입에 따라 업데이트 빈도 조절
**새 이름**: `update_projectiles_conditional()`

---

## ✅ #8. FUN_1000_8135 - 함수 포인터 디스패처

**주소**: 1000:8135
**크기**: 6 bytes

### 코드
```c
void FUN_1000_8135(void) {
  /* WARNING: Treating indirect jump as call */
  (*(code *)*(undefined2 *)0x18d0)();
}
```

### 분석

**함수 포인터 테이블**: 0x18d0
- 2바이트 함수 포인터 읽기 → 호출

**역할**: 동적 함수 디스패치
**새 이름**: `dispatch_function_at_18d0()`

---

## ✅ #9. FUN_1000_034b - 반복 호출 루프

**주소**: 1000:034b
**크기**: 16 bytes

### 코드
```c
void FUN_1000_034b(void) {
  uint uVar1 = *(uint *)0x4c;
  do {
    FUN_1000_499b();
    uVar1 -= 2;
  } while (0x1569 < uVar1);
}
```

### 분석

**반복 횟수**: 0x4c 값에 따라 결정
- 0x4c > 0x1569 (5481)까지 반복

**역할**: 특정 함수를 여러 번 호출 (메모리 복사?)
**새 이름**: `repeat_function_499b()`

---

## ✅ #10. FUN_1000_462a - 타이머 기반 이벤트

**주소**: 1000:462a
**크기**: 21 bytes

### 코드
```c
void FUN_1000_462a(void) {
  if ((*(char *)0x4410 != '\0') && (*(char *)0x4410 < *(char *)0x4419)) {
    FUN_1000_56d7();
    *(char *)0x4410 = *(char *)0x4410 + '\x01';
  }
}
```

### 분석

**타이머**:
- 0x4410: 현재 카운터
- 0x4419: 최대값
- 카운터 < 최대값일 때만 실행

**역할**: 애니메이션 프레임 카운터 또는 타이머 이벤트
**새 이름**: `timer_event_trigger()`

---

## ⭐ #11. FUN_1000_0518 - 플레이어 입력 → 카메라 스크롤

**주소**: 1000:0518
**크기**: 118 bytes

### 코드
```c
void FUN_1000_0518(void) {
  uVar2 = FUN_1000_0590();  // 조이스틱/키보드 입력 읽기
  int iVar1 = (int)((ulong)uVar2 >> 0x10);

  *(undefined1 *)0x16be = 0;  // 수평 방향
  *(undefined1 *)0x16bf = 0;  // 수직 방향

  // 수평 입력 처리
  if (0x2f < (int)uVar2) {
    *(char *)0x16be += 1;  // 오른쪽
    in_BX--;
  }
  if (in_BX < 0xd) {
    *(char *)0x16be -= 1;  // 왼쪽
  }

  // 수직 입력 처리
  if (0x20 < in_CX) {
    *(char *)0x16bf += 1;  // 아래
    iVar1--;
  }
  if (iVar1 < 0x10) {
    *(char *)0x16bf -= 1;  // 위
  }

  // 스크롤 제한 확인 및 실행
  if (*(char *)0x16be != '\0') {
    if (*(char *)0x16be < '\0') {
      if (*(int *)0x38d3 < *(int *)0xf396) {
        FUN_1000_82f4();  // 왼쪽 스크롤
      }
    } else if (*(int *)0xf396 < *(int *)0x38d5) {
      FUN_1000_8277();  // 오른쪽 스크롤
    }
  }

  if (*(char *)0x16bf != '\0') {
    if (*(char *)0x16bf < '\0') {
      if (*(int *)0x38d7 < *(int *)0xf398) {
        FUN_1000_822a();  // 위 스크롤
      }
    } else if (*(int *)0xf398 < *(int *)0x38d9) {
      FUN_1000_81c2();  // 아래 스크롤
    }
  }
}
```

### 분석

**입력 → 스크롤 파이프라인**:
1. FUN_1000_0590(): 조이스틱/키보드 입력
2. 방향 계산 (0x16be, 0x16bf)
3. 스크롤 제한 확인 (0x38d3~0x38d9)
4. 스크롤 함수 호출

**메모리 맵**:
- 0xf396, 0xf398: 현재 스크롤 위치 (X, Y)
- 0x38d3, 0x38d5: X 스크롤 제한 (최소, 최대)
- 0x38d7, 0x38d9: Y 스크롤 제한 (최소, 최대)

**역할**: 플레이어 입력으로 화면 스크롤 제어
**새 이름**: `handle_input_and_scroll_camera()`

---

## ✅ #12. FUN_1000_4640 - 게임 상태 관리자

**주소**: 1000:4640
**크기**: 32 bytes

### 코드
```c
void FUN_1000_4640(void) {
  if (*(int *)0x16b0 < 2) {  // 게임 상태 체크
    FUN_1000_4660();
    FUN_1000_474a();
  }
  FUN_1000_468e();
  FUN_1000_46cd();
  FUN_1000_4711();
  FUN_1000_471e();
  FUN_1000_475d();
  FUN_1000_4780();
}
```

### 분석

**게임 서브시스템 호출**:
- 8개 헬퍼 함수 순차 실행
- 첫 2개는 조건부 (0x16b0 < 2)

**역할**: 게임 상태에 따라 여러 서브시스템 업데이트
**새 이름**: `update_game_subsystems()`

---

## ⭐ #13. FUN_1000_3fe0 - 스테이지 디스패처

**주소**: 1000:3fe0
**크기**: 18 bytes (ASM only)

### 코드
```asm
1000:3fe0  MOV AL,[0x38d0]      ; 현재 스테이지 번호 읽기
1000:3fe3  DEC AL               ; -1 (0-based index)
1000:3fe5  SHL AL,0x1           ; * 2 (word 오프셋)
1000:3fe7  MOV BL,AL
1000:3fe9  XOR BH,BH            ; BX = (stage-1)*2
1000:3feb  ADD BX,0x3ff4        ; 점프 테이블 주소 계산
1000:3fef  MOV DX,CS:[BX]       ; 함수 포인터 읽기
1000:3ff2  JMP DX               ; 스테이지별 함수로 점프
```

### 분석

**점프 테이블**: 0x3ff4
- 각 스테이지마다 2바이트 함수 포인터
- 예: Stage 1 → [0x3ff4], Stage 2 → [0x3ff6], ...

**역할**: 현재 스테이지에 맞는 함수로 분기
**새 이름**: `dispatch_stage_function()`

---

## ✅ #14. FUN_1000_05e9 - 난수 생성기 / 타이머

**주소**: 1000:05e9
**크기**: 26 bytes

### 코드
```c
void FUN_1000_05e9(void) {
  int iVar3;
  if (*(uint *)0x16b2 == 0) {
    iVar3 = -0x4d;  // 초기값
  } else {
    long lVar1 = (ulong)*(uint *)0x16b2 * 0x4d;  // * 77
    uint uVar4 = (uint)((ulong)lVar1 >> 0x10);
    uint uVar2 = (uint)lVar1;
    iVar3 = uVar2 - uVar4;
    if (uVar2 < uVar4) {
      iVar3++;
    }
  }
  *(int *)0x16b2 = iVar3;
}
```

### 분석

**알고리즘**:
- 0x16b2 = seed
- seed = 0 ? -77 : complex_multiply(seed, 77)
- Linear Congruential Generator (LCG) 변형

**역할**: 난수 생성 또는 프레임 타이머 업데이트
**새 이름**: `update_random_seed_or_timer()`

---

## 📊 메인 루프 완전 분석 완료!

### 14개 함수 요약

| # | 함수 | 크기 | 역할 | 중요도 |
|---|------|------|------|--------|
| 1 | FUN_1000_03ca | 32 | 엔티티 렌더링 준비 | ⭐⭐⭐ |
| 2 | FUN_1000_20fb | 32 | 투사체 렌더링 준비 | ⭐⭐⭐ |
| 3 | FUN_1000_039e | 44 | 화면 경계 체크 | ⭐⭐ |
| 4 | FUN_1000_211b | 32 | 투사체 로직 업데이트 | ⭐⭐⭐ |
| 5 | **FUN_1000_029a** | **177** | **깊이 정렬 렌더링** | ⭐⭐⭐⭐⭐ |
| 6 | FUN_1000_03ea | 40 | 격프레임 엔티티 업데이트 | ⭐⭐ |
| 7 | FUN_1000_213b | 77 | 조건부 투사체 업데이트 | ⭐⭐ |
| 8 | FUN_1000_8135 | 6 | 함수 포인터 디스패처 | ⭐ |
| 9 | FUN_1000_034b | 16 | 반복 호출 루프 | ⭐ |
| 10 | FUN_1000_462a | 21 | 타이머 이벤트 | ⭐ |
| 11 | **FUN_1000_0518** | **118** | **입력 → 스크롤** | ⭐⭐⭐⭐ |
| 12 | FUN_1000_4640 | 32 | 게임 상태 관리자 | ⭐⭐⭐ |
| 13 | **FUN_1000_3fe0** | **18** | **스테이지 디스패처** | ⭐⭐⭐⭐ |
| 14 | FUN_1000_05e9 | 26 | 난수/타이머 | ⭐⭐ |

### 🔥 최우선 발견

#### 1. FUN_1000_029a - 메인 렌더링 루프
- **깊이 정렬 렌더링** 구현
- 투사체 + 엔티티 통합 렌더링
- 방향별 스프라이트 선택 로직

#### 2. FUN_1000_0518 - 입력/스크롤 시스템
- 조이스틱/키보드 입력
- 카메라 스크롤 제한 체크
- 4방향 스크롤 함수 호출

#### 3. FUN_1000_3fe0 - 스테이지 시스템
- 점프 테이블 @ 0x3ff4
- 스테이지별 함수 분기

### 메모리 맵 업데이트

| 주소 | 용도 | 발견 |
|------|------|------|
| 0x004c | 반복 카운터 | #9 |
| 0x156a | 렌더 리스트 포인터 | #5 |
| 0x16b0 | 프레임 카운터 / 게임 상태 | #6, #12 |
| 0x16b2 | 난수 시드 / 타이머 | #14 |
| 0x16be | 수평 입력 플래그 | #11 |
| 0x16bf | 수직 입력 플래그 | #11 |
| 0x16c0 | 시작 깊이 레이어 | #5 |
| 0x16c2 | 끝 깊이 레이어 | #5 |
| 0x16c6~0x176e | 엔티티 배열 (7개×24B) | #1, #5, #6 |
| 0x1697 | 엔티티 X 좌표 | #3 |
| 0x169b | 방향 플래그 | #3 |
| 0x18d0 | 함수 포인터 테이블 | #8 |
| 0x3530, 0x3532 | 투사체 타입 플래그 | #7 |
| 0x3542~0x35ae | 투사체 배열 (6개×18B) | #2, #4, #5, #7 |
| 0x38d0 | 현재 스테이지 번호 | #13 |
| 0x38d3~0x38d9 | 스크롤 제한 (X/Y min/max) | #11 |
| 0x3ff4 | 스테이지 점프 테이블 | #13 |
| 0x4410 | 타이머 카운터 | #10 |
| 0x4419 | 타이머 최대값 | #10 |
| 0x4664 | 스프라이트 데이터 포인터 | #5 |
| 0xf396, 0xf398 | 화면 스크롤 (X, Y) | #3, #11 |

---

## 🎉 결과

### 완료 항목
- ✅ 14개 함수 완전 분석
- ✅ 메인 렌더링 루프 발견 (FUN_1000_029a)
- ✅ 입력/스크롤 시스템 파악 (FUN_1000_0518)
- ✅ 스테이지 디스패처 발견 (FUN_1000_3fe0)
- ✅ 메모리 맵 30+ 주소 추가

### 주요 발견
1. **깊이 정렬 렌더링**: 뒤→앞 레이어 순회, Z-ordering 구현
2. **방향별 스프라이트**: 4방향 (2,4,6,8) + 각 방향마다 8바이트 오프셋
3. **스크롤 시스템**: 입력 → 제한 체크 → 스크롤 실행
4. **스테이지 시스템**: 점프 테이블 기반 분기

---

## 🚀 다음 단계

### 즉시 분석 가능
1. ⭐ **FUN_1000_48e0 분석** - 실제 Blit 함수 (029a에서 호출)
2. **FUN_1000_0590 분석** - 입력 읽기 함수 (0518에서 호출)
3. **0x3ff4 점프 테이블 덤프** - 스테이지별 함수 목록
4. **Phase 4 완료 문서화**

### Phase 5 준비
- 155개 함수 → C++ 클래스 재설계
- 렌더링 엔진 구현
- 입력/스크롤 시스템 구현
- 스테이지 로더 구현

---

**마지막 업데이트**: 2025-11-24
**소요 시간**: 약 2시간
**분석 완료율**: 100% (14/14)
